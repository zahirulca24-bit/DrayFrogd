name: Scalping 15m and Intraday priority verifier

on:
  push:
    branches:
      - fix/scalping-15m-intraday-priority

permissions:
  contents: write

jobs:
  verify-and-commit:
    if: ${{ github.actor != 'github-actions[bot]' }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout branch
        uses: actions/checkout@v4
        with:
          ref: fix/scalping-15m-intraday-priority
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install backend dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Apply deterministic patch
        run: python tools/apply_scalping_15m_intraday_priority.py

      - name: Compile backend
        run: python -m compileall -q app tests

      - name: Run focused profile selection tests
        run: |
          python -m unittest tests.test_scanner_integration -v
          python -m unittest tests.test_signal_pipeline -v
          python -m unittest tests.test_profile_selection_priority -v

      - name: Run full backend suite
        run: python -m unittest discover -s tests -p 'test_*.py'

      - name: Set up Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        working-directory: frontend
        run: npm ci

      - name: Frontend TypeScript check
        working-directory: frontend
        run: npm run lint

      - name: Frontend production build
        working-directory: frontend
        run: npm run build

      - name: Commit verified product changes
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git rm -f tools/apply_scalping_15m_intraday_priority.py .github/workflows/scalping-15m-intraday-priority-verify.yml
          git add app tests
          git commit -m 'Use 15m Scalping trend and prefer Intraday signals'
          git push origin HEAD:fix/scalping-15m-intraday-priority
